/// Border-radius tokens from Stitch `DESIGN.md` §Rounded.
class AppRadii {
  const AppRadii._();

  /// 4px — sm (wall placement bars, beveled catchlight).
  static const double sm = 4;

  /// 8px — DEFAULT (buttons, selectors).
  static const double md = 8;

  /// 12px — md (inputs, modals).
  static const double lg = 12;

  /// 16px — lg (HUD cards, modals).
  static const double xl = 16;

  /// 24px — xl (board outer).
  static const double boardOuter = 24;

  /// 9999px — full (circular pawns, movement tokens).
  static const double full = 9999;
}
